import Vue from 'vue'
import { 
    Button,
    Collapse,
    CollapseItem,
    Icon,
    Col,
    Row,
    Sidebar,
    SidebarItem,
} from 'vant'
Vue
.use(Button)
.use(Collapse)
.use(CollapseItem)//Collapse 折叠面板
.use(Icon)//Icon 图标
.use(Col)//Icon 图标
.use(Row)//layout 布局
.use(Sidebar)
.use(SidebarItem)//Sidebar 侧边导航